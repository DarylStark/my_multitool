"""Module with one dummy test.

Since this package has no unit tests (I don't see how I could test the CLI app
in a Unit Test), we define one dummy test. This is only to make sure the
GitHub Workflow succeeds.
"""


def test_dummy() -> None:
    """Dummy test.

    Is always succeeding.
    """
    assert True
